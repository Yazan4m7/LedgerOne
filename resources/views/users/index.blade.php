@extends('layouts.app') @section('title','Users') @section('content')<h1>Users</h1><div class="card"><form method="post" action="{{ route('users.store') }}">@csrf<div class="grid"><label>Name<input name="name" required></label><label>Email<input type="email" name="email" required></label><label>Password<input type="password" name="password" minlength="12" required></label><label>Roles<select name="role_ids[]" multiple required>@foreach($roles as $r)<option value="{{ $r->id }}">{{ $r->name }}</option>@endforeach</select></label></div><button>Create user</button></form></div><table><tr><th>Name</th><th>Email</th><th>Roles</th><th>Active</th></tr>@foreach($users as $u)<tr><td>{{ $u->name }}</td><td>{{ $u->email }}</td><td>{{ $u->roles->pluck('name')->join(', ') }}</td><td>{{ $u->active?'Yes':'No' }}</td></tr>@endforeach</table>@endsection
