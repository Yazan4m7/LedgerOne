@extends('layouts.app') @section('title',$bill->bill_number) @section('content')<h1>{{ $bill->bill_number }}</h1><div class="card">{{ $bill->contact->name }} — {{ $bill->bill_date->format('Y-m-d') }} — {{ $bill->currency->code }} {{ $bill->total }} — {{ $bill->status }}</div><table><tr><th>Description</th><th>Qty</th><th>Price</th><th>Subtotal</th><th>Tax</th></tr>@foreach($bill->lines as $l)<tr><td>{{ $l->description }}</td><td>{{ $l->quantity }}</td><td>{{ $l->unit_price }}</td><td>{{ $l->line_subtotal }}</td><td>{{ $l->tax_amount }}</td></tr>@endforeach</table>@if($bill->status==='draft'&&auth()->user()->hasPermission('purchases.post'))<form method="post" action="{{ route('purchases.post',$bill) }}" style="margin-top:12px">@csrf<button>Post bill</button></form>@endif@endsection
