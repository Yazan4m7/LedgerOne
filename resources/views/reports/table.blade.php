@extends('layouts.app') @section('title',$title) @section('content')<h1>{{ $title }}</h1>@if(!$rows)<p>No rows.</p>@else @php($headers=array_keys($rows[0]))<table><thead><tr>@foreach($headers as $h)<th>{{ str($h)->replace('_',' ')->title() }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($headers as $h)<td>{{ $row[$h] }}</td>@endforeach</tr>@endforeach</tbody></table>@endif @endsection
