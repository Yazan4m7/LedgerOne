@extends('layouts.app') @section('title','Payments') @section('content')<div class="actions"><h1 style="margin-right:auto">Payments</h1><a href="{{ route('payments.create',['type'=>'receipt']) }}">Receive</a><a href="{{ route('payments.create',['type'=>'supplier_payment']) }}">Pay supplier</a></div><table><tr><th>No.</th><th>Date</th><th>Contact</th><th>Type</th><th>Amount</th><th>Status</th><th></th></tr>@foreach($payments as $p)<tr><td>{{ $p->payment_number }}</td><td>{{ $p->payment_date->format('Y-m-d') }}</td><td>{{ $p->contact->name }}</td><td>{{ $p->type }}</td><td>{{ $p->amount }}</td><td>{{ $p->status }}</td><td>@if($p->status==='posted'&&auth()->user()->hasPermission('payments.void'))<form method="post" action="{{ route('payments.void',$p) }}">@csrf<input type="date" name="date" required><input name="reason" required placeholder="Reason"><button class="danger">Void</button></form>@endif</td></tr>@endforeach</table>{{ $payments->links() }}@endsection
