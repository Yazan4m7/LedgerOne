@extends('layouts.app') @section('title','Contacts') @section('content')<h1>Contacts</h1><div class="card"><form method="post" action="{{ route('contacts.store') }}">@csrf<div class="grid"><label>Type<select name="type"><option>customer</option><option>supplier</option><option>both</option></select></label><label>Name<input name="name" required></label><label>Tax no.<input name="tax_number"></label><label>Email<input type="email" name="email"></label><label>Phone<input name="phone"></label><label>AR account<select name="receivable_account_id"><option value="">Company default</option>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} {{ $a->name_en }}</option>@endforeach</select></label><label>AP account<select name="payable_account_id"><option value="">Company default</option>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} {{ $a->name_en }}</option>@endforeach</select></label></div><button>Create</button></form></div><table><tr><th>Name</th><th>Type</th><th>Tax</th><th>Email</th><th>Phone</th><th>Active</th></tr>@foreach($contacts as $c)<tr><td>{{ $c->name }}</td><td>{{ $c->type }}</td><td>{{ $c->tax_number }}</td><td>{{ $c->email }}</td><td>{{ $c->phone }}</td><td>{{ $c->active?'Yes':'No' }}</td></tr>@endforeach</table>@endsection
