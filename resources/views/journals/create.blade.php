@extends('layouts.app') @section('title','New Journal') @section('content')<h1>New Journal</h1><form method="post" action="{{ route('journals.store') }}">@csrf<div class="card grid"><label>Date<input type="date" name="entry_date" value="{{ date('Y-m-d') }}" required></label><label>Description<input name="description"></label></div><div class="card"><h3>Lines</h3><div id="journal-lines"><template><div class="line-row"><label>Account<select name="lines[__INDEX__][account_id]">@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} {{ $a->name_en }}</option>@endforeach</select></label><label>Debit<input name="lines[__INDEX__][debit]" value="0.000"></label><label>Credit<input name="lines[__INDEX__][credit]" value="0.000"></label><label>Description<input name="lines[__INDEX__][description]"></label><span></span><button class="danger" data-remove-line>Remove</button></div></template>@for($i=0;$i<2;$i)<div class="line-row"><label>Account<select name="lines[{{ $i }}][account_id]">@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->code }} {{ $a->name_en }}</option>@endforeach</select></label><label>Debit<input name="lines[{{ $i }}][debit]" value="0.000"></label><label>Credit<input name="lines[{{ $i }}][credit]" value="0.000"></label><label>Description<input name="lines[{{ $i }}][description]"></label><span></span><button class="danger" data-remove-line>Remove</button></div>@endfor</div><button class="secondary" data-add-line data-target="#journal-lines">Add line</button></div><button>Save draft</button></form>@endsection
