@extends('layouts.app') @section('title','FX') @section('content')<h1>Foreign Exchange</h1><div class="grid"><div class="card"><h3>Rate</h3><form method="post" action="{{ route('fx.store') }}">@csrf<label>Currency<select name="currency_id">@foreach($currencies as $c)<option value="{{ $c->id }}">{{ $c->code }}</option>@endforeach</select></label><label>Date<input type="date" name="rate_date" required></label><label>Rate to base currency<input name="rate" required></label><label>Source<input name="source"></label><button>Save</button></form></div><div class="card"><h3>Period-end revaluation</h3><form method="post" action="{{ route('fx.revalue') }}">@csrf<label>Date<input type="date" name="date" required></label><button>Run revaluation</button></form></div></div><table><tr><th>Date</th><th>Currency</th><th>Rate</th><th>Source</th></tr>@foreach($rates as $r)<tr><td>{{ $r->rate_date->format('Y-m-d') }}</td><td>{{ $r->currency->code }}</td><td>{{ $r->rate }}</td><td>{{ $r->source }}</td></tr>@endforeach</table>{{ $rates->links() }}@endsection
