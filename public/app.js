document.addEventListener('click',e=>{if(e.target.matches('[data-add-line]')){e.preventDefault();const box=document.querySelector(e.target.dataset.target);const tpl=box.querySelector('template');const i=box.querySelectorAll('.line-row').length;box.insertAdjacentHTML('beforeend',tpl.innerHTML.replaceAll('__INDEX__',i));}if(e.target.matches('[data-remove-line]')){e.preventDefault();e.target.closest('.line-row')?.remove();}});
